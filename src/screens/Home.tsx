import { Text } from "react-native"
import { Screen } from "../components/base/Screen"

export const Home: React.FC<{}> = () => {
    return (
        <Screen>
            <Text>Home</Text>
        </Screen>
    )
}